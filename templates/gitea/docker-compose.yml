services:
  gitea:
    image: gitea/gitea:1
    restart: unless-stopped
    environment:
      USER_UID: "1000"
      USER_GID: "1000"
    ports:
      - "${PORT}:3000"
      - "${SSH_PORT}:22"
    volumes:
      - giteadata:/data
volumes:
  giteadata:
