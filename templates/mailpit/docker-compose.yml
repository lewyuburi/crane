services:
  mailpit:
    image: axllent/mailpit:latest
    restart: unless-stopped
    environment:
      MP_MAX_MESSAGES: "5000"
      MP_DATABASE: /data/mailpit.db
    ports:
      - "${PORT}:8025"
      - "${SMTP_PORT}:1025"
    volumes:
      - mailpitdata:/data
volumes:
  mailpitdata:
