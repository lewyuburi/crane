services:
  searxng:
    image: searxng/searxng:latest
    restart: unless-stopped
    environment:
      SEARXNG_BASE_URL: http://localhost:${PORT}/
    ports:
      - "${PORT}:8080"
    volumes:
      - searxng:/etc/searxng
volumes:
  searxng:
